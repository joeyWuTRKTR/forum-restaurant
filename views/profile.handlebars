<div class="row">
  <div class="col-4">
    <img src="" alt="">
  </div>
  <div class="col-8">
    <h3>{{user.name}}</h3>
    <h5>{{user.email}}</h5>
    <a href="/users/{{user.id}}/edit" class="btn btn-primary">Edit</a>
  </div>
</div>